package com.luv.couple.notify

import android.content.Context

/** Stub: kein google-services.json → kein Firebase/FCM in diesem Build. */
object LuvFcm {
    fun registerIfAvailable(context: Context) {
        // no-op
    }

    fun clearLocalToken(context: Context) {
        // no-op
    }
}
